using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//not specifying a thread type makes sqlite function in serialized mode, making it thread-safe

public class SQLiteDataReader : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
